# ⚠️ Carpeta archivada (obsoleta)

Esta era la app móvil **Flutter**, que leía datos de **Firebase**. Con el pivote a la
arquitectura v3 (backend Spring + PostgreSQL, sin Firebase) quedó **obsoleta**.

Se reemplaza por la app **React Native + Expo** en [`../WeatherStation_MOVIL/`](../WeatherStation_MOVIL/),
que consume exclusivamente el backend Spring vía API REST + JWT.

Se conserva **solo como referencia de UI/UX**. No compilar ni usar.

Ver la especificación de la app nueva en
[`../specs/002-app-movil/`](../specs/002-app-movil/).
